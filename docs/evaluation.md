# Evaluation status

The golden-dataset runner and regression quality gate are deliberately deferred until Phase 5.
Phase 1 verifies deterministic ingestion, dense retrieval behavior, refusal, citations, and API
contracts. No quality metrics are claimed before a real corpus and verified question set exist.

